# [PW-D05] Define Audiobookshelf browser onboarding contract

| Field | Value |
|---|---|
| Gitea issue | `slarti/backlog#270` |
| Source URL | https://gitea.hl.maier.wtf/slarti/backlog/issues/270 |
| State | `open` |
| Labels | `Systemarchitektur`, `sub-task` |
| Updated | `2026-07-22T18:24:50+02:00` |

## Exact Ticket Contract

Parent: #253

## Objective

Pin exact Audiobookshelf 2.35.1 journeys, stable selectors, controlled library data and runtime prerequisites before low-cost service test implementation.

## Authoritative Source Paths

- `yeraziael/slartis-backlog@5f2de959a4fb21b07405c7be65066b3093fb14af:docs/epics/playwright/` (planning baseline; verify the same documents are on the default branch before implementation)
- `Homelab/ACP@6b5e8ec58cde1e193b9310c01fc68b6885de8df5` / release `v0.1.0-draft`: `README.md`, `FINDINGS/PILOT-57.md`, `FINDINGS/TEMPLATE.md`
- `Homelab/Architecture/AGENTS.md` at the executor's base commit
- `docs/epics/playwright/testing.md` Audiobookshelf Acceptance
- `docs/epics/audiobookshelf/requirements.md` and `contracts.md`
- `Homelab/Architecture/docs/AUDIOBOOKSHELF.md`, `docs/audiobookshelf-oidc.md`, `pi/compose/audiobookshelf.yml`

## In Scope

- Define unauthenticated, login/logout, library/playback, user/admin/denied journeys with exact expected UI states.
- Identify stable ARIA/test-id selectors or explicit fallback selectors.
- Define a non-personal controlled media item and operator readiness evidence.

## Explicit Non-Goals

- No Playwright test code, runtime data creation, account provisioning or service configuration change.

## Prerequisites

- PW-I11 merged; current deployed version and OIDC configuration can be evidenced without secrets.

## Permitted Files Or Repository Areas

- `docs/decisions/playwright/audiobookshelf-onboarding.md` only.

Everything else is forbidden unless this ticket is amended before execution. Generated `Makefile`, `.gitea/workflows/ci.yaml`, and `.pre-commit-config.yaml` may only change through `python3 ci-generate.py` when listed above.

## Normative Implementation Contract

- Every browser assertion MUST map to a canonical service requirement and lower-layer boundary.
- Controlled data MUST contain no personal content and have deterministic title/library expectations.
- Missing stable selector or controlled data MUST be an explicit prerequisite, not left to executor exploration.

## Acceptance Criteria

- PW-I12 through PW-I15 have exact journeys, selectors, data, roles and expected states.
- Live prerequisites and operator actions are separated from repository changes.

## Required Tests

- Validate all internal links and any selector inventory format.
- Run repo lint/test with no live browser mutation.

## Required CI Validation

- Existing CI green; no live service test is added.
- From the repository root, run `make lint`, `make test`, and `git diff --check origin/main...HEAD`.
- The required Gitea Actions jobs for the PR head MUST be green. An absent job is an evidence deficiency, not a pass.

## ACP Phase Transitions

- `READY -> IN_PROGRESS`: dependencies and source-merge gate verified; instruction commit recorded before edits.
- `IN_PROGRESS -> EVIDENCE_READY`: scoped implementation and all required local tests complete.
- `EVIDENCE_READY -> WAITING_FOR_PR_REVIEW`: PR exists; transport validation confirms URL, head SHA, changed-file list, CI runs and evidence are accessible; one final Gitea evidence comment is posted.
- `WAITING_FOR_PR_REVIEW -> IN_REVIEW -> COMPLETED`: independent reviewer evaluates the exact head SHA; completion requires a SHA-bound accepting verdict, green required CI, merge by Eddie/operator, and ticket closure.
- Any pre-completion state may become `BLOCKED`; record the category and stop rather than inventing a workaround. `CHANGES_REQUESTED` returns to `IN_PROGRESS` with prior findings retained.

ACP `v0.1.0-draft` has no normative state machine in `SPEC/`. These are the repository control states required by `Homelab/Architecture/AGENTS.md` and accepted Pilot 57 findings; they MUST NOT be represented as a new ACP specification.

## Required ACP Evidence

The final Gitea evidence comment MUST contain:

- `instruction_file`, `instruction_commit`, `instruction_reloaded`, `pr_head_commit`, `changed_files`, `ci_run_ids`, and `review_target: pr` exactly as required by `Homelab/Architecture/AGENTS.md`.
- Base commit, branch, Gitea PR URL, exact test commands and outcomes, required CI job names/run URLs, and an acceptance-criterion-to-evidence matrix.
- Declaration of whether runtime, deployment, DNS, secrets, accounts, containers or external systems were mutated.
- Rollback statement and the complete findings record.
- Transport validation: PR head equals the evidenced SHA; changed files equal the permitted scope; CI and evidence links resolve for the reviewer.

Approval is valid only for the evidenced head SHA. A later push invalidates it and requires re-review.

## Required Findings Record

Record findings in the internal Gitea ticket work journal and summarise them in the implementation PR. Use every category below, writing `none observed` when empty:

- `contract ambiguity`
- `missing prerequisite`
- `implementation defect`
- `test defect`
- `infrastructure failure`
- `evidence deficiency`
- `model limitation`
- `process friction`
- `generic ACP candidate`
- `domain-specific learning`

Unresolved contract ambiguity, missing prerequisite, implementation defect, test defect, infrastructure failure, evidence deficiency, or model limitation blocks completion. Process friction blocks only when it prevents contract or evidence compliance. Generic ACP candidates are copied to the five-ticket checkpoint, not directly encoded in ACP. Playwright-specific learning remains in this ticket, its PR, or the canonical Playwright plan and MUST NOT be added to ACP.

## Rollback Expectations

- Revert decision PR; no controlled data or service state changed.

## Definition Of Done

- Decision independently reviewed and merged.
- Exactly one focused Gitea PR implements this ticket; no merge or deployment is performed by the executor.
- The independent reviewer has accepted the exact PR head SHA and Eddie/operator has merged it.

## Dependencies

- #269

## Recommended Model

Sol high only. This ticket resolves architecture; do not delegate it to a low-cost implementation model.

## Escalation Conditions

- Controlled test library does not exist.
- Deployed version differs from documented 2.35.1.
- Required stable selectors cannot be established without upstream/service change.
- OIDC runtime setup remains pending.

## GitHub Reviewer Notification

After entering `WAITING_FOR_PR_REVIEW`, post one concise notification in the designated GitHub Playwright reviewer thread. Include only the internal Gitea ticket reference, Gitea PR reference, scope, tests/CI, ACP evidence location, findings summary and exact head SHA. Do not reproduce the internal backlog on GitHub.
