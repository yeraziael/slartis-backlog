diff --git a/.gitea/workflows/ci.yaml b/.gitea/workflows/ci.yaml
index 21a00f2..fdc3fe0 100644
--- a/.gitea/workflows/ci.yaml
+++ b/.gitea/workflows/ci.yaml
@@ -33,3 +33,5 @@ jobs:
             grep -v -E '^\.[/\\]*(\.gitea|node_modules|\.git)' | \
             grep -v -E '(token|secret|password)[= ][<[]' || \
             { echo "Possible secrets found in committed files"; exit 1; }
+      - name: schema-test
+        run: python3 SCHEMAS/test_schemas.py
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 227b6a2..53c2ba6 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -12,6 +12,17 @@
 - Added `EXAMPLES/trigger-contract-invalid.json` — 4 invalid Trigger Contract examples
 - Added `DECISIONS/001-execution-trigger-contracts.md` — ADR-001 for this change
 
+## v0.2.1-draft
+
+- **Breaking:** `action_ref` version suffix is now mandatory (was optional)
+- **Breaking:** `constraints`, `rollback`, `success_criteria` are now required top-level fields in Execution Contract
+- **Breaking:** `webhook.secret` renamed to `webhook.secret_ref` with normative resolution requirement
+- Added immutable identity model: `(trigger_id, activation.version)` is the immutable identity pair
+- Added conditional required sub-fields for `schedule`: cron type requires `cron`, schedule type requires `interval_seconds`
+- Added `SCHEMAS/test_schemas.py` — schema-level test harness with 12+ validation scenarios
+- Updated CI workflow to run schema tests
+- Added schema-level violation examples for unversioned action_ref and missing required fields
+
 ## v0.1.0-draft
 
 - Initial repository setup
diff --git a/DECISIONS/001-execution-trigger-contracts.md b/DECISIONS/001-execution-trigger-contracts.md
index e3d128a..e13ef30 100644
--- a/DECISIONS/001-execution-trigger-contracts.md
+++ b/DECISIONS/001-execution-trigger-contracts.md
@@ -28,7 +28,7 @@ provider-independent contracts that can be realised by any executor.
 
 ## Decision
 
-Add two normative specifications to ACP v0.2.0-draft:
+Add two normative specifications to ACP v0.2.1-draft:
 
 1. **Execution Contract** (`SPEC/execution-contract.md`):
    - Defines what a Principal must provide for a single deterministic action.
diff --git a/EXAMPLES/execution-contract-invalid.json b/EXAMPLES/execution-contract-invalid.json
index ffd4037..ee90da5 100644
--- a/EXAMPLES/execution-contract-invalid.json
+++ b/EXAMPLES/execution-contract-invalid.json
@@ -11,6 +11,18 @@
       "parameters": {
         "command": "ls -la"
       },
+      "constraints": {
+        "timeout_seconds": 300,
+        "max_retries": 0,
+        "concurrency_key": "test",
+        "idempotency_key": "exec-missing-toplevel-000"
+      },
+      "rollback": {
+        "strategy": "none"
+      },
+      "success_criteria": [
+        { "type": "exit_code", "target": "0" }
+      ],
       "evidence": {
         "require_output": true
       }
@@ -21,7 +33,7 @@
   {
     "scenario": "Unknown action (not in executor registry)",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "execution_id": "exec-invalid-action-001",
       "action": "nonexistent.operation",
       "version": "9.9.9",
@@ -33,6 +45,18 @@
       "parameters": {
         "target": "/etc/passwd"
       },
+      "constraints": {
+        "timeout_seconds": 60,
+        "max_retries": 0,
+        "concurrency_key": "test",
+        "idempotency_key": "exec-invalid-action-001"
+      },
+      "rollback": {
+        "strategy": "none"
+      },
+      "success_criteria": [
+        { "type": "exit_code", "target": "0" }
+      ],
       "evidence": {
         "require_output": true
       }
@@ -43,7 +67,7 @@
   {
     "scenario": "Precondition failure: tool version mismatch",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "execution_id": "exec-version-mismatch-002",
       "action": "fileops.copy",
       "version": "2.0.0",
@@ -63,6 +87,18 @@
           "message": "fileops.copy v2.0.0 must be registered"
         }
       ],
+      "constraints": {
+        "timeout_seconds": 120,
+        "max_retries": 1,
+        "concurrency_key": "fileops",
+        "idempotency_key": "exec-version-mismatch-002"
+      },
+      "rollback": {
+        "strategy": "none"
+      },
+      "success_criteria": [
+        { "type": "exit_code", "target": "0" }
+      ],
       "evidence": {
         "require_output": true
       }
@@ -73,7 +109,7 @@
   {
     "scenario": "Missing parameters for action",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "execution_id": "exec-missing-params-003",
       "action": "issue.create",
       "version": "1.0.0",
@@ -83,6 +119,18 @@
         "created_at": "2026-07-25T12:00:00Z"
       },
       "parameters": {},
+      "constraints": {
+        "timeout_seconds": 60,
+        "max_retries": 0,
+        "concurrency_key": "issues",
+        "idempotency_key": "exec-missing-params-003"
+      },
+      "rollback": {
+        "strategy": "none"
+      },
+      "success_criteria": [
+        { "type": "exit_code", "target": "0" }
+      ],
       "evidence": {
         "require_output": true
       }
@@ -93,7 +141,7 @@
   {
     "scenario": "Insufficient permissions: path outside allowed_paths",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "execution_id": "exec-permission-denied-004",
       "action": "fileops.read",
       "version": "1.0.0",
@@ -109,6 +157,18 @@
         "allowed_paths": ["/home/lydia/workspace/"],
         "requires_approval": false
       },
+      "constraints": {
+        "timeout_seconds": 30,
+        "max_retries": 0,
+        "concurrency_key": "fileops",
+        "idempotency_key": "exec-permission-denied-004"
+      },
+      "rollback": {
+        "strategy": "none"
+      },
+      "success_criteria": [
+        { "type": "exit_code", "target": "0" }
+      ],
       "evidence": {
         "require_output": true
       }
diff --git a/EXAMPLES/execution-contract-valid.json b/EXAMPLES/execution-contract-valid.json
index 51cdd03..26e0de0 100644
--- a/EXAMPLES/execution-contract-valid.json
+++ b/EXAMPLES/execution-contract-valid.json
@@ -1,5 +1,5 @@
 {
-  "acp_version": "0.2.0-draft",
+  "acp_version": "0.2.1-draft",
   "execution_id": "exec-20260725-ollama-prompt-a1b2c3d4",
   "action": "ollama.prompt",
   "version": "1.2.0",
diff --git a/EXAMPLES/trigger-contract-invalid.json b/EXAMPLES/trigger-contract-invalid.json
index 93f6677..257cb75 100644
--- a/EXAMPLES/trigger-contract-invalid.json
+++ b/EXAMPLES/trigger-contract-invalid.json
@@ -2,7 +2,7 @@
   {
     "scenario": "Unregistered action_ref",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "trigger_id": "trigger-invalid-action-001",
       "action_ref": "nonexistent.operation:1.0.0",
       "provenance": {
@@ -20,9 +20,9 @@
     "reason": "No executor registered for 'nonexistent.operation:1.0.0'"
   },
   {
-    "scenario": "Missing schedule for cron trigger type",
+    "scenario": "Missing schedule for trigger_type cron",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "trigger_id": "trigger-missing-schedule-002",
       "action_ref": "ops.backup.paperless:1.0.0",
       "provenance": {
@@ -39,7 +39,7 @@
   {
     "scenario": "Invalid cron expression",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "trigger_id": "trigger-bad-cron-003",
       "action_ref": "ops.health.pi:1.0.0",
       "provenance": {
@@ -59,7 +59,7 @@
   {
     "scenario": "Eddie contains execution logic (normative violation)",
     "contract": {
-      "acp_version": "0.2.0-draft",
+      "acp_version": "0.2.1-draft",
       "trigger_id": "trigger-eddie-logic-004",
       "action_ref": "custom.script:1.0.0",
       "provenance": {
@@ -76,5 +76,25 @@
     },
     "expected_rejection": "resolution",
     "reason": "Trigger contracts must reference registered actions via action_ref. Shell commands, script paths, and inline logic are not valid action_ref values."
+  },
+  {
+    "scenario": "Unversioned action_ref (schema violation)",
+    "contract": {
+      "acp_version": "0.2.1-draft",
+      "trigger_id": "trigger-no-version-005",
+      "action_ref": "ops.backup.paperless",
+      "provenance": {
+        "principal": "slarti",
+        "trace_id": "trace-no-version",
+        "created_at": "2026-07-25T12:00:00Z"
+      },
+      "trigger_type": "cron",
+      "schedule": {
+        "cron": "0 3 * * *"
+      },
+      "parameters": {}
+    },
+    "expected_rejection": "schema",
+    "reason": "action_ref 'ops.backup.paperless' is missing mandatory version suffix ':semver'"
   }
 ]
diff --git a/EXAMPLES/trigger-contract-valid.json b/EXAMPLES/trigger-contract-valid.json
index e5f637d..e4c02e0 100644
--- a/EXAMPLES/trigger-contract-valid.json
+++ b/EXAMPLES/trigger-contract-valid.json
@@ -1,5 +1,5 @@
 {
-  "acp_version": "0.2.0-draft",
+  "acp_version": "0.2.1-draft",
   "trigger_id": "trigger-backup-paperless-daily",
   "action_ref": "ops.backup.paperless:1.0.0",
   "provenance": {
diff --git a/SCHEMAS/execution-contract.json b/SCHEMAS/execution-contract.json
index 0d56e14..1540a39 100644
--- a/SCHEMAS/execution-contract.json
+++ b/SCHEMAS/execution-contract.json
@@ -2,12 +2,12 @@
   "$schema": "http://json-schema.org/draft-07/schema#",
   "$id": "https://gitea.hl.maier.wtf/Homelab/ACP/SCHEMAS/execution-contract.json",
   "title": "ACP Execution Contract",
-  "description": "Formal contract between a Principal (Slarti/Operator) and an Executor (Lydia). Version 0.2.0-draft.",
+  "description": "Formal contract between a Principal (Slarti/Operator) and an Executor (Lydia). Version 0.2.1-draft.",
   "type": "object",
   "properties": {
     "acp_version": {
       "type": "string",
-      "const": "0.2.0-draft"
+      "const": "0.2.1-draft"
     },
     "execution_id": {
       "type": "string",
@@ -131,6 +131,9 @@
     "version",
     "provenance",
     "parameters",
+    "constraints",
+    "rollback",
+    "success_criteria",
     "evidence"
   ],
   "additionalProperties": false
diff --git a/SCHEMAS/test_schemas.py b/SCHEMAS/test_schemas.py
new file mode 100644
index 0000000..6dd073d
--- /dev/null
+++ b/SCHEMAS/test_schemas.py
@@ -0,0 +1,124 @@
+#!/usr/bin/env python3
+"""Test harness for ACP JSON Schemas.
+
+Validates schema syntax, valid examples pass, and schema-level rejections fail.
+Run from repo root: python3 SCHEMAS/test_schemas.py
+"""
+import json
+import sys
+from pathlib import Path
+
+import jsonschema
+
+
+REPO = Path(__file__).resolve().parent.parent
+SCHEMAS = REPO / "SCHEMAS"
+EXAMPLES = REPO / "EXAMPLES"
+
+
+def load(path):
+    with open(path) as f:
+        return json.load(f)
+
+
+def validate_invalid_examples(schema, examples_path):
+    """Run each invalid example scenario through the schema.
+
+    Scenarios with expected_rejection == "schema" must FAIL schema validation.
+    Other scenarios are informational.
+    """
+    errors = []
+    try:
+        examples = load(examples_path)
+    except json.JSONDecodeError as e:
+        return [f"  FAIL: {examples_path.name} is not valid JSON: {e}"]
+
+    for i, entry in enumerate(examples):
+        scenario = entry.get("scenario", f"entry-{i}")
+        contract = entry.get("contract", {})
+        expected_rejection = entry.get("expected_rejection", "")
+
+        try:
+            jsonschema.validate(contract, schema)
+            passed = True
+        except jsonschema.ValidationError:
+            passed = False
+
+        if expected_rejection == "schema":
+            if passed:
+                errors.append(
+                    f"  FAIL [{scenario}]: expected schema rejection but contract passed validation"
+                )
+            else:
+                print(f"  OK [{scenario}]: correctly rejected by schema")
+        elif expected_rejection == "validation":
+            if not passed:
+                print(f"  OK [{scenario}]: schema also catches this validation case")
+            else:
+                print(f"  INFO [{scenario}]: runtime-only validation (passes schema)")
+        else:
+            if not passed:
+                print(f"  INFO [{scenario}]: schema also catches this {expected_rejection} rejection (stricter schema)")
+            else:
+                print(f"  OK [{scenario}]: passes schema — rejection is runtime-level ({expected_rejection})")
+
+    return errors
+
+
+def main():
+    exit_code = 0
+
+    for schema_name, examples_valid, examples_invalid in [
+        ("trigger-contract.json", "trigger-contract-valid.json", "trigger-contract-invalid.json"),
+        ("execution-contract.json", "execution-contract-valid.json", "execution-contract-invalid.json"),
+    ]:
+        schema_path = SCHEMAS / schema_name
+        valid_path = EXAMPLES / examples_valid
+        invalid_path = EXAMPLES / examples_invalid
+
+        if not schema_path.exists():
+            print(f"  FAIL: {schema_path} not found")
+            exit_code = 1
+            continue
+
+        try:
+            schema = load(schema_path)
+        except json.JSONDecodeError as e:
+            print(f"  FAIL: {schema_name} is not valid JSON: {e}")
+            exit_code = 1
+            continue
+
+        try:
+            jsonschema.Draft7Validator.check_schema(schema)
+        except jsonschema.SchemaError as e:
+            print(f"  FAIL: {schema_name} is not a valid Draft-07 schema: {e}")
+            exit_code = 1
+            continue
+        print(f"  OK: {schema_name} is valid Draft-07 schema")
+
+        if valid_path.exists():
+            try:
+                contract = load(valid_path)
+            except json.JSONDecodeError as e:
+                print(f"  FAIL: {valid_path.name} is not valid JSON: {e}")
+                exit_code = 1
+                continue
+
+            try:
+                jsonschema.validate(contract, schema)
+                print(f"  OK: {valid_path.name} passes schema")
+            except jsonschema.ValidationError as e:
+                print(f"  FAIL: {valid_path.name} should pass schema but got: {e.message}")
+                exit_code = 1
+
+        if invalid_path.exists():
+            errors = validate_invalid_examples(schema, invalid_path)
+            for err in errors:
+                print(err)
+                exit_code = 1
+
+    return exit_code
+
+
+if __name__ == "__main__":
+    sys.exit(main())
diff --git a/SCHEMAS/trigger-contract.json b/SCHEMAS/trigger-contract.json
index 59d74d3..fdb4dfd 100644
--- a/SCHEMAS/trigger-contract.json
+++ b/SCHEMAS/trigger-contract.json
@@ -2,12 +2,12 @@
   "$schema": "http://json-schema.org/draft-07/schema#",
   "$id": "https://gitea.hl.maier.wtf/Homelab/ACP/SCHEMAS/trigger-contract.json",
   "title": "ACP Trigger Contract",
-  "description": "Formal contract between a Principal (Slarti/Operator) and an Orchestrator (Eddie). Version 0.2.0-draft.",
+  "description": "Formal contract between a Principal (Slarti/Operator) and an Orchestrator (Eddie). Version 0.2.1-draft.",
   "type": "object",
   "properties": {
     "acp_version": {
       "type": "string",
-      "const": "0.2.0-draft"
+      "const": "0.2.1-draft"
     },
     "trigger_id": {
       "type": "string",
@@ -15,7 +15,7 @@
     },
     "action_ref": {
       "type": "string",
-      "pattern": "^[a-z][a-z0-9._/-]{2,63}(:[0-9]+\\.[0-9]+\\.[0-9]+)?$"
+      "pattern": "^[a-z][a-z0-9._/-]{2,63}:[0-9]+\\.[0-9]+\\.[0-9]+$"
     },
     "provenance": {
       "type": "object",
@@ -58,7 +58,7 @@
       "properties": {
         "path": { "type": "string", "minLength": 1 },
         "method": { "type": "string", "default": "POST" },
-        "secret": { "type": "string" }
+        "secret_ref": { "type": "string", "pattern": "^[A-Za-z0-9._/-]+$" }
       },
       "required": ["path"],
       "additionalProperties": false
@@ -137,9 +137,25 @@
   "allOf": [
     {
       "if": {
-        "properties": { "trigger_type": { "enum": ["schedule", "cron"] } }
+        "properties": { "trigger_type": { "const": "cron" } }
       },
-      "then": { "required": ["schedule"] }
+      "then": {
+        "required": ["schedule"],
+        "properties": {
+          "schedule": { "required": ["cron"] }
+        }
+      }
+    },
+    {
+      "if": {
+        "properties": { "trigger_type": { "const": "schedule" } }
+      },
+      "then": {
+        "required": ["schedule"],
+        "properties": {
+          "schedule": { "required": ["interval_seconds"] }
+        }
+      }
     },
     {
       "if": {
diff --git a/SPEC/execution-contract.md b/SPEC/execution-contract.md
index 4433f4b..5669dfc 100644
--- a/SPEC/execution-contract.md
+++ b/SPEC/execution-contract.md
@@ -1,6 +1,6 @@
 # Execution Contract (ACP v0.2)
 
-**Version:** 0.2.0-draft
+**Version:** 0.2.1-draft
 **Status:** Draft
 **Supersedes:** Implicit execution conventions in `lydia/home-repo`
 
@@ -82,7 +82,7 @@ to execute without interpretation, inference, or tool selection.
 ### `acp_version`
 - **Type:** `string` (const)
 - **Required:** yes
-- **Value:** `"0.2.0-draft"`
+- **Value:** `"0.2.1-draft"`
 - **Description:** ACP specification version to which this contract conforms.
 
 ### `execution_id`
@@ -156,8 +156,11 @@ Each precondition object:
 
 ### `constraints`
 - **Type:** `object`
-- **Required:** no
-- **Description:** Execution boundaries.
+- **Required:** yes
+- **Description:** Execution boundaries. Every contract MUST specify explicit
+  timeouts, retry limits, and concurrency/idempotency keys. Default values
+  are never assumed — the Principal must choose deliberate bounds for every
+  execution.
 
 | Field | Type | Required | Default | Description |
 |-------|------|----------|---------|-------------|
@@ -168,9 +171,12 @@ Each precondition object:
 
 ### `rollback`
 - **Type:** `object`
-- **Required:** no
-- **Description:** Rollback strategy for failures. If absent, no automatic
-  rollback is available.
+- **Required:** yes
+- **Description:** Rollback strategy for failures. Every contract MUST declare
+  what happens on failure. Even actions that cannot be reverted MUST declare
+  `strategy: none` explicitly. The Executor MUST execute the rollback strategy
+  before returning the `FAILED` result if the executed action left state changes
+  behind.
 
 | Field | Type | Required | Description |
 |-------|------|----------|-------------|
@@ -180,9 +186,12 @@ Each precondition object:
 
 ### `success_criteria`
 - **Type:** `array` of `object`
-- **Required:** no
-- **Description:** Explicit criteria that define success. If absent, a
-  non-zero exit status or error response constitutes failure.
+- **Required:** yes
+- **Description:** Explicit criteria that define success. Every contract MUST
+  declare at least one success criterion. A non-zero exit status or error
+  response is always a failure regardless of criteria, but without explicit
+  criteria the Executor cannot determine what constitutes a successful outcome.
+  Example default: `[{ "type": "exit_code", "target": "0" }]`.
 
 Each criterion object:
 
diff --git a/SPEC/trigger-contract.md b/SPEC/trigger-contract.md
index db8d966..ce3292f 100644
--- a/SPEC/trigger-contract.md
+++ b/SPEC/trigger-contract.md
@@ -1,6 +1,6 @@
 # Trigger Contract (ACP v0.2)
 
-**Version:** 0.2.0-draft
+**Version:** 0.2.1-draft
 **Status:** Draft
 **Supersedes:** Eddie's implicit cron definitions and ad-hoc webhook registrations
 
@@ -22,11 +22,15 @@ modify the execution. Eddie contains no execution logic.
    valid Execution Contracts from trigger definitions and submit them to the
    appropriate executor.
 
-3. **Trigger changes require a new version.** A trigger definition is immutable
-   after activation. Parameter changes, schedule changes, or action changes
-   require a new trigger version.
+3. **Immutable identity model.** Every trigger definition is uniquely identified
+   by the immutable pair `(trigger_id, activation.version)`. Changing any field
+   of an active trigger — parameters, schedule, action_ref, or trigger_type —
+   requires a new `activation.version`. Eddie MUST reject registration attempts
+   where the same `(trigger_id, version)` pair already exists with different
+   content. This ensures deterministic audit trails: every execution can be
+   traced to exactly one trigger version.
 
-4. **Missed runs are Eddie's accountability.** Eddie must handle scheduling
+5. **Missed runs are Eddie's accountability.** Eddie must handle scheduling
    gaps, retries, and deadline violations according to the contract without
    requiring operator intervention for routine misses.
 
@@ -82,7 +86,7 @@ modify the execution. Eddie contains no execution logic.
 ### `acp_version`
 - **Type:** `string` (const)
 - **Required:** yes
-- **Value:** `"0.2.0-draft"`
+- **Value:** `"0.2.1-draft"`
 
 ### `trigger_id`
 - **Type:** `string`
@@ -95,9 +99,9 @@ modify the execution. Eddie contains no execution logic.
 ### `action_ref`
 - **Type:** `string`
 - **Required:** yes
-- **Pattern:** `^[a-z][a-z0-9._/-]{2,63}(:[0-9]+\.[0-9]+\.[0-9]+)?$`
-- **Description:** Reference to a registered action with optional version
-  suffix. Format: `<action>[:<version>]`. Example: `ollama.prompt:1.2.0`.
+- **Pattern:** `^[a-z][a-z0-9._/-]{2,63}:[0-9]+\.[0-9]+\.[0-9]+$`
+- **Description:** Reference to a registered action with mandatory version
+  suffix. Format: `<action>:<version>`. Example: `ollama.prompt:1.2.0`.
   Eddie MUST resolve this against the executor's registered capabilities and
   reject the trigger if no executor provides the action.
 
@@ -159,7 +163,7 @@ modify the execution. Eddie contains no execution logic.
 |-------|------|----------|-------------|
 | `path` | `string` | yes | URL path suffix (e.g. `/webhooks/my-trigger`) |
 | `method` | `string` | no | HTTP method to accept (default: `POST`) |
-| `secret` | `string` (reference) | no | Reference to a shared secret for HMAC validation |
+| `secret_ref` | `string` | no | Reference to a shared secret for HMAC validation. Format: `secret:<name>` or `vault:<path>`. Eddie MUST resolve the reference to a concrete secret value before registration succeeds. An unresolvable reference MUST cause a `resolution` rejection. |
 
 ### `parameters`
 - **Type:** `object`
diff --git a/VERSION b/VERSION
index 9618463..a2124a6 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-0.2.0-draft
+0.2.1-draft
