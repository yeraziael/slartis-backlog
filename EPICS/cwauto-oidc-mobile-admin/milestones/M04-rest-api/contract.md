# Milestone Contract

> **STATUS (2026-07-20): DISCARDED.** REST-API-Erweiterung entfällt. Der Fork
> wurde verworfen; eine Eigenimplementierung ist ohne hinreichenden Mehrwert
> nicht vorgesehen.

- Erstelle zuerst ein API-Inventar des verwendeten CWA-Upstreamstands: Endpunkte, Methoden, Payloads, Authentifizierung, Rollen, Stabilitaet und Dokumentation.
- Bewerte, ob die vorhandene API die Homelab-Anwendungsfaelle abdeckt.
- Falls keine geeignete API existiert, implementiere eine kleine versionierte REST API unter einem klaren Prefix.
- Falls eine API existiert, erweitere nur belegte Luecken und vermeide parallele Endpunkte.
- API-Antworten duerfen keine Secrets, Sessiondaten oder interne Pfade offenlegen.
